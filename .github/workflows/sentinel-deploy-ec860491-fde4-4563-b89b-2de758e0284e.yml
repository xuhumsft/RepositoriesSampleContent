name: Deploy Content to TestWkSpace [ec860491-fde4-4563-b89b-2de758e0284e]
# Note: This workflow will deploy everything in the root directory.
# To deploy content only from a specific path (for example SentinelContent):
#   1. Add the target path to the "paths" property like such
#    paths:
#    - 'SentinelContent/**'
#    - '!.github/workflows/**'
#    - '.github/workflows/sentinel-deploy-ec860491-fde4-4563-b89b-2de758e0284e.yml'
#   2. Append the path to the directory environment variable below
#       directory: '${{ github.workspace }}/SentinelContent'

on:
  push:
    branches: [ add-bicep-templates ]
    paths:
    - '**'
    - '!.github/workflows/**'  # this filter prevents other workflow changes from triggering this workflow
    - '.github/workflows/sentinel-deploy-ec860491-fde4-4563-b89b-2de758e0284e.yml'

jobs:
  deploy-content:
    runs-on: windows-latest
    env:
      resourceGroupName: 'newecotestrg2'
      workspaceName: 'TestWkSpace'
      workspaceId: 'd67b16c8-7718-48cc-bb81-6b1d47ccbc3f'
      directory: '${{ github.workspace }}'
      cloudEnv: 'AzurePPE'
      contentTypes: 'AnalyticsRule'
      branch: 'add-bicep-templates'
      sourceControlId: 'ec860491-fde4-4563-b89b-2de758e0284e'
      rootDirectory: '${{ github.workspace }}'
      githubAuthToken: ${{ secrets.GITHUB_TOKEN }}
      smartDeployment: 'true'
    permissions:
      contents: write
      id-token: write # Require write permission to Fetch an OIDC token.

    steps:
    - name: Login to Azure (Attempt 1)
      continue-on-error: true
      id: login1
      uses: azure/login@v2
      if: ${{ env.cloudEnv == 'AzureCloud' }}
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_ec860491fde44563b89b2de758e0284e }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_ec860491fde44563b89b2de758e0284e }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_ec860491fde44563b89b2de758e0284e }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Wait 30 seconds if login attempt 1 failed
      if: ${{ env.cloudEnv == 'AzureCloud' && steps.login1.outcome=='failure' }}
      run: powershell Start-Sleep -s 30

    - name: Login to Azure (Attempt 2)
      continue-on-error: true
      id: login2
      uses: azure/login@v2
      if: ${{ env.cloudEnv == 'AzureCloud' && steps.login1.outcome=='failure' }}
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_ec860491fde44563b89b2de758e0284e }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_ec860491fde44563b89b2de758e0284e }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_ec860491fde44563b89b2de758e0284e }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Wait 30 seconds if login attempt 2 failed
      if: ${{ env.cloudEnv == 'AzureCloud' && steps.login2.outcome=='failure' }}
      run: powershell Start-Sleep -s 30

    - name: Login to Azure (Attempt 3)
      continue-on-error: false
      id: login3
      uses: azure/login@v2
      if: ${{ env.cloudEnv == 'AzureCloud' && steps.login2.outcome=='failure'  }}
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_ec860491fde44563b89b2de758e0284e }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_ec860491fde44563b89b2de758e0284e }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_ec860491fde44563b89b2de758e0284e }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Checkout
      uses: actions/checkout@v3

    - name: Deploy Content to Microsoft Sentinel
      uses: azure/powershell@v2
      with:
        azPSVersion: 'latest'
        inlineScript: |
          ${{ github.workspace }}//.github/workflows/azure-sentinel-deploy-ec860491-fde4-4563-b89b-2de758e0284e.ps1